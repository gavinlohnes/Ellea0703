export function renderCycle(app) {
    app.innerHTML = `
        <div class="panel">
            <div class="section-title">Cycle</div>
            <p>Your daily + weekly rhythm.</p>
        </div>
    `;
}
