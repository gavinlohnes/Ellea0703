export function renderSync(app) {
    app.innerHTML = `
        <div class="panel">
            <div class="section-title">Sync Ritual</div>
            <p>Weekly review + preload.</p>
        </div>
    `;
}
